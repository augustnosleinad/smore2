<li id="session-<%= session.id %>">
  <span class="content">Worked on for <%= session.duration %> minutes</span>
  <span class="timestamp"><%= time_ago_in_words(session.created_at) %> ago.
  </span>
</li>